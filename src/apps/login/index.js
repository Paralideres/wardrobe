import React from 'react';
import ReactDOM from 'react-dom';

const rootEl = document.getElementById('root');

const App = () => (<p>This is the login form</p>);

ReactDOM.render(
  <App />,
  rootEl
);
